# ??????????

LONG / SHORT TRACKER ???????? Cloudflare Workers Cron Triggers??????????????Codex ?????????????????????

## ????

```text
Cloudflare Cron??? 00:00 UTC?
  ? POST ?? Sites /api/refresh
  ? SEC EDGAR ?? 13F + ??/????????
  ? ?? Sites D1 ???????
  ? ?? 13F ????????? Resend ???
  ? Cloudflare Cron Events / Workers Logs ????
```

????? UTC+8??? `0 0 * * *` ???? 08:00?Cloudflare Cron ?? UTC?????????????? 15 ?????

13F ????????????????????????????????????????? SEC ?????????

## ??????

?????`wrangler.jsonc`

```jsonc
{
  "triggers": { "crons": ["0 0 * * *"] },
  "vars": {
    "SITES_REFRESH_URL": "https://holdings-lens-cn.zvcdg28tfj.chatgpt.site"
  },
  "secrets": {
    "required": ["RESEND_API_KEY", "SITES_REFRESH_BEARER_TOKEN", "OPERATIONS_ALERT_EMAIL"]
  }
}
```

`SITES_REFRESH_BEARER_TOKEN` ??? Sites ? SIWC bypass token????? Cloudflare encrypted secret ?????? GitHub???? `.env` ???

`OPERATIONS_ALERT_EMAIL` ??????????????????? encrypted secret ????????????????????8 ????????????????????????????????????? Resend idempotency key?????????????

## ?????

???Node.js 22.13+?Cloudflare ?????? Wrangler??? Sites ?????? Resend ?????

```powershell
npm ci
npx wrangler login
npx wrangler secret put SITES_REFRESH_BEARER_TOKEN
npx wrangler secret put RESEND_API_KEY
npx wrangler secret put OPERATIONS_ALERT_EMAIL
npm test
npx tsc --noEmit
npx wrangler deploy
```

? `SITES_REFRESH_BEARER_TOKEN` ??????? Sites ??? bypass token???????? Sites ?????????????? token?????? SIWC bypass token??????? token ?????? token??????????? `wrangler secret put SITES_REFRESH_BEARER_TOKEN`?

Cloudflare ??????**Workers & Pages ? long-short-tracker ? Settings ? Triggers ? Cron Triggers**???? `0 0 * * *`?

## ??

### `POST /api/refresh`

??????????????????? Sites ???????? Worker ???????

```http
POST https://holdings-lens-cn.zvcdg28tfj.chatgpt.site/api/refresh
OAI-Sites-Authorization: Bearer <SITES_REFRESH_BEARER_TOKEN>
Accept: application/json
Cache-Control: no-store
```

??????????

- `results`?8 ???? SEC ?????`updated` ????????`unchanged` ???????
- `publicSignals`?????????????????????
- `pendingAlertRetry`?????????????
- `publicSignalDelivery`???????????????????????
- `skipped: true, reason: "rate_limited"`?24 ??????????????????

?? HTTP 500??????? 8????? `status=error`?????????????????? Cron Event ?????

### `GET /api/status`

??????????????????????????? `dataReady`?`emailReady`?`trackedFunds`?`snapshotFunds`?`lastRefreshAt` ? `refreshIntervalHours`?

### `GET /api/holdings`

??????????????????????? 13F ???????

### `GET /api/admin/summary`

???????????????????????????????????????????????

## ????

??????????????????

```powershell
$headers = @{
  "OAI-Sites-Authorization" = "Bearer $env:SITES_REFRESH_BEARER_TOKEN"
  "Accept" = "application/json"
}
Invoke-RestMethod -Method Post -Uri "https://holdings-lens-cn.zvcdg28tfj.chatgpt.site/api/refresh" -Headers $headers
```

???? scheduled handler?

```powershell
npx wrangler dev --test-scheduled
Invoke-RestMethod "http://localhost:8787/cdn-cgi/handler/scheduled?cron=0+0+*+*+*&format=json"
```

?? Worker ?? `/__scheduled` ??? 404????????????

## ???????

???????

```powershell
npx wrangler tail long-short-tracker
```

???? Cloudflare ????? **long-short-tracker ? Logs ? Cron Events**???????? `cloud_refresh_succeeded`?????? `cloud_refresh_failed`???????????? `cloud_refresh_failure_alert_sent`??????????????? `cloud_refresh_failure_alert_failed`???? Cloudflare ?????

?????

1. `401/403`??? Sites token ?????????? `SITES_REFRESH_BEARER_TOKEN`?
2. HTTP 500????? Sites Worker Logs????? D1?SEC?Resend ?????
3. ?????? 8 ???? SEC EDGAR ????? CIK?
4. ??????????? RSS?X API?????????????????????????????????
5. ??????????? `alert_deliveries` ?????????? Resend ???API key ??????

## ??????

?? `wrangler.jsonc` ? `triggers.crons` ?????????

- ?????? 08:00?`0 0 * * *`
- ?????? 08:00?20:00?`0 */12 * * *`
- ?????? 09:00?`0 1 * * *`

?????? Cloudflare Cron?GitHub Actions ???/Codex ??????????????????? 24 ???????????????????

## ????

1. ? Sites ???????? SIWC bypass token?? token ??????
2. ?? `npx wrangler secret put SITES_REFRESH_BEARER_TOKEN`?
3. ?????? `/api/refresh`??????? 401/403?
4. ? Cloudflare Cron Events ?????????????

????????????? GitHub ??????? token ? Resend API key?
